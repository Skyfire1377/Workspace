package ciclingRotation;
public class CiclingRotationFinder{
	public boolean check(String pattern, String testing){
		int position = 0;	
		for(int i =0; i<pattern.length(); i++){
			String s = pattern.substring(i, i);
			position = testing.indexOf(s);
			if(position){

			}
		}
	}	
}
